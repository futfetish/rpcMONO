import { LoginForm } from "@/features/loginForm";
import { FC } from "react";

const Login : FC = () => {
    return (
        <div>
            <LoginForm />
        </div>
    )
}

export default Login